{
  "name": "compose",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "mobile:sync:android": "npm run build && npx cap sync android",
    "mobile:open:android": "npx cap open android",
    "mobile:sync:ios": "npm run build && npx cap sync ios",
    "mobile:open:ios": "npx cap open ios",
    "mobile:doctor": "node scripts/mobile-doctor.mjs",
    "build:android-web": "npm run build",
    "build:ios-web": "npm run build",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "android:buildprep": "npm run mobile:sync:android",
    "ios:sync": "npx cap sync ios",
    "ios:open": "npx cap open ios",
    "ios:buildprep": "npm run mobile:sync:ios",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:webkit": "playwright test --project=webkit",
    "test:e2e:webkit:headed": "playwright test --project=webkit --headed",
    "playwright:install": "playwright install webkit"
  },
  "devDependencies": {
    "@capacitor/android": "^8.2.0",
    "@capacitor/cli": "^8.2.0",
    "@capacitor/core": "^8.2.0",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/ios": "^8.2.0",
    "@capacitor/share": "^8.0.1",
    "@playwright/test": "^1.54.2",
    "vite": "^7.1.5"
  }
}
